- error = @organize.errors.include?(:name)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :name, :class => 'control-label'
  .controls
    =f.text_field :name, :class => 'form-control input-large input-with-feedback', :autofocus => true
    %span.help-inline=error ? f.error_message_on(:name, :class => 'text-error') : pat(:example)
- error = @organize.errors.include?(:description)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :description, :class => 'control-label'
  .controls
    =f.text_field :description, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:description, :class => 'text-error') : pat(:example)
- error = @organize.errors.include?(:owner)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :owner, :class => 'control-label'
  .controls
    =f.text_field :owner, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:owner, :class => 'text-error') : pat(:example)
- error = @organize.errors.include?(:zuzi_type)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :zuzi_type, :class => 'control-label'
  .controls
    =f.text_field :zuzi_type, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:zuzi_type, :class => 'text-error') : pat(:example)
- error = @organize.errors.include?(:node_id)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :node_id, :class => 'control-label'
  .controls
    =f.text_field :node_id, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:node_id, :class => 'text-error') : pat(:example)
- error = @organize.errors.include?(:integer)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :integer, :class => 'control-label'
  .controls
    =f.text_field :integer, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:integer, :class => 'text-error') : pat(:example)

.form-actions
  =f.submit pat(:save), :class => 'btn btn-primary'
  &nbsp;
  =f.submit pat(:save_and_continue), :class => 'btn btn-info', :name => 'save_and_continue'
  &nbsp;
  =link_to pat(:cancel), url(:organizes, :index), :class => 'btn btn-default'