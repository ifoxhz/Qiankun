%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:room_billings, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:room_billings, :new)

.tabs-content
  = form_for :room_billing, url(:room_billings, :create), :class => 'form-horizontal' do |f|
    =partial 'room_billings/form', :locals => { :f => f }
