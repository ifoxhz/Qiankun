%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:areas, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:areas, :new)

.tabs-content
  = form_for :area, url(:areas, :create), :class => 'form-horizontal' do |f|
    =partial 'areas/form', :locals => { :f => f }
