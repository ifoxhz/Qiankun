%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:room_families, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:room_families, :new)

.tabs-content
  = form_for :room_family, url(:room_families, :create), :class => 'form-horizontal' do |f|
    =partial 'room_families/form', :locals => { :f => f }
